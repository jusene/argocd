apiVersion: apisix.apache.org/v2beta3
kind: ApisixTls
metadata:
  name: dash-tls
  namespace: kubernetes-dashboard
spec:
  hosts:
  - k8s.hho-inc.com
  secret:
    name: hho-inc.com
    namespace: default